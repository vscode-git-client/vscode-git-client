// Generated by split-class.mjs
import type { GitService } from './index';

export async function canApplyPatchToWorkingTree(this: GitService, patch: string): Promise<boolean> {
  if (!patch.trim()) {
        return false;
      }
  
      try {
        await this.runGitWithStdin(['apply', '--check', '--3way', '--whitespace=nowarn'], patch);
        return true;
      } catch {
        return false;
      }
}
