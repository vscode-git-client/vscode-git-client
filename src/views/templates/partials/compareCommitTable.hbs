<section class="card">
  <div class="section-banner" data-side="{{side}}" data-total="{{total}}" data-ref="{{ref}}">Only in {{ref}} ({{total}})</div>
  <div class="table-wrap">
    {{> partials/commitTable rows=rows}}
  </div>
</section>
