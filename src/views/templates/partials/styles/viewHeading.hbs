    .view-heading-title {
      margin: 0;
      font-size: {{titleSize}};
      font-weight: {{#if titleWeight}}{{titleWeight}}{{else}}600{{/if}};
    }
    .view-heading-hint {
      margin: 0;
      font-size: {{#if hintSize}}{{hintSize}}{{else}}11px{{/if}};
      color: var(--muted);
    }
